# 프로젝트 작업 지침

## 커밋과 PR 작성

- 중요한 기능 추가, 버그 수정, 설정·작업 지침 변경을 완료하면 관련 검증 후 변경 단위별로 커밋한다. 이 범위의 커밋은 매번 사용자 확인을 요청하지 않는다.
- 커밋 메시지는 변경 목적을 구체적으로 작성하고, 관련 GitHub 이슈가 있으면 번호를 포함한다.
- PR 제목과 본문은 한글로 작성한다. 본문에는 변경 목적, 최종 동작, 검증 결과와 확인하지 못한 사항을 설명하고 관련 이슈를 연결한다.
- 사용자가 푸시와 PR 생성을 요청하면 커밋된 변경을 작업 브랜치로 푸시하고 한글 PR을 생성한다.

## GitHub 이슈 관리

- 작업 중 발견한 버그, 개선 과제, 의존성 호환성 문제, 후속 작업은 GitHub 이슈에 한글로 기록한다. 사용자가 이슈 생성과 분류를 요청했으므로 이 범위에서 매번 확인을 요청하지 않는다.
- 새 이슈를 만들기 전에 기존 이슈를 검색한다. 같은 문제가 있으면 기존 이슈에 근거와 진행 상황을 추가한다.
- 제목에는 구체적인 문제나 개선 목표를 적는다. 본문에는 문제와 영향, 재현 방법 또는 확인 근거, 기대 동작, 해결 방향과 완료 조건을 포함한다. 확인되지 않은 내용은 추정이라고 명시한다.
- 일시적인 로컬 실행 오류나 바로 해결된 단순 오타는 별도 이슈로 만들지 않는다. 추적할 가치가 있는 문제를 기록하며 비밀 값과 개인정보는 포함하지 않는다.
- 유형은 기존 라벨을 사용한다: `bug`(오류), `enhancement`(기능·개선), `documentation`(문서), `dependencies`(의존성). 해당하는 라벨을 함께 적용한다.
- 우선순위는 `priority: high`(서비스 장애·게시 불가·긴급 수정), `priority: medium`(일반 오류·필요한 개선), `priority: low`(긴급하지 않은 개선) 중 하나로 분류한다.
- 관련 커밋과 PR에 이슈 번호를 연결한다. PR 설명과 이슈의 진행·종료 사유도 한글로 작성한다. 해결을 검증한 후 이슈를 닫고, 보류·중복·불필요한 이슈는 이유를 남긴다.
- 작업 완료 시 생성·갱신·종료한 이슈가 있다면 링크와 처리 내용을 사용자에게 알린다.
